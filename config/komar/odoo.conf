[options]
; This is the password that allows database operations:
admin_passwd = #@AllohOnMyside02032024

db_host = db
db_port = 5432
db_user = komar
db_password = @Hasbunalloh2024
data_dir = /var/lib/odoo
addons_path = /mnt/extra-addons/

; Jumlah worker, cukup untuk 3 user
workers = 2

; Batasan penggunaan ram per worker 1.25 GB hard 1GB soft dalam bytes 
limit_memory_hard = 1342177280
limit_memory_soft = 1073741824

; Worker tambahan untuk cron job
max_cron_threads = 1

; Batas jumlah request per worker
limit_request = 4096

; Batasan waktu eksekusi max CPU max Real
limit_time_cpu = 60
limit_time_real = 120

;proxy_mode = True
log_level = info

gevent_port = 7202

